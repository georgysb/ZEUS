ZEUS system supports up to 4 GPIO modules, provided A2, A3 port select jumpers are set differently.
Be advised, GPIO module isn't tested yet.

Back board coupler is used to join 2 ZEUS6 back boards into 11 slot system.
